/** @type {import('next').NextConfig} */
const nextConfig = {
  trailingSlash: false,
  reactStrictMode: true,
  reactStrictMode: true,

  images: {
    loader: "default",
    path: "",
    domains: ["localhost"],
  },
};

export default nextConfig;
